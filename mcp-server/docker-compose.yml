services:
  liferadar-mcp:
    build:
      context: ../
      dockerfile: mcp-server/Dockerfile
    image: liferadar-mcp:latest
    container_name: liferadar-mcp
    environment:
      LIFE_RADAR_API_URL: http://host.docker.internal:8000
    restart: unless-stopped
